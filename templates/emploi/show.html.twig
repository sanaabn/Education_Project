<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Les Emplois</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <h1 class="text-center text-primary mb-4">Les Emplois</h1>
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="text-primary">Les Emplois</h1>
            <div>
                <a href="{{ path('app_show_Salle') }}" class="btn btn-info">📌 Voir les salles</a>
                <a href="{{ path('app_add_emploi') }}" class="btn btn-success">➕ Ajouter un emploi</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-bordered table-striped text-center shadow-sm">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Jour</th>
                        <th>Heure Début</th>
                        <th>Heure Fin</th>
                        <th>Salle</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for emploi in emplois %}
                    <tr>
                        <td>{{ emploi.id }}</td>
                        <td>{{ emploi.jour }}</td>
                        <td>{{ emploi.hrDeb|date('H:i') }}</td>
                        <td>{{ emploi.hrFin|date('H:i') }}</td>
                        <td>{% if emploi.Salle %}{{ emploi.Salle.nom }}{% else %}Non assignée{% endif %}</td>
                        <td>
                            <a href="{{ path('app_edit_emp', { 'id': emploi.id }) }}" class="btn btn-sm btn-warning">Modifier</a>
                            <a href="{{ path('app_delete_emp', { 'id': emploi.id }) }}" class="btn btn-sm btn-danger">Supprimer</a>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="6" class="text-muted">Aucun emploi trouvé.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
